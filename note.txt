递归下降法:
值语义:
  1、脱离对象 基于对象的编程
  2、它的析构函数是确定的
对象语义:
  1、禁止拷贝构造函数
  2、依赖对象 面向对象的编程

RAII:资源获取初始化

auto_ptr            该指针资源不共享，但是可以转移
shared_ptr          该指针资源共享，内部维护一个引用计数
weak_ptr            该指针为弱指针通常配合shared_ptr，实现一个回收循环引用的资源
scoped_ptr          该指针资源不共享，但是不可以转移

strtod:字符串转double